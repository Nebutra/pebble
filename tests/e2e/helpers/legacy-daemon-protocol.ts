// These tests exercise the legacy terminal daemon until their Tauri lifecycle
// replacements land; keep its fixture version out of the canonical runtime contract.
export const LEGACY_DAEMON_PROTOCOL_VERSION = 18
